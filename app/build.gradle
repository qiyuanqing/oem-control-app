plugins {
    id 'com.android.application'
}

android {
    namespace 'com.o0ai.control'
    compileSdk 34

    defaultConfig {
        applicationId 'com.o0ai.control'
        minSdk 27
        targetSdk 27
        versionCode 1
        versionName '0.1.0'
    }

    buildTypes {
        debug {
            versionNameSuffix '-debug'
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
